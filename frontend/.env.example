# ADIVA Frontend — Local Environment
# Copy this to .env.local for local dev (never commit .env.local)

# Base URL for backend API — leave empty to use Vite proxy (recommended for local dev)
# Vite will forward /api/* to http://localhost:8000
VITE_API_BASE_URL=
